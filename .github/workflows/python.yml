name: Python CI

on:
  push:
    branches:
      - main
      - master

  pull_request:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Create Test Environment
        run: |
          mkdir -p config
          cat > config/.env << EOF
          API_KEY=2APH%UQQc8HfG7q;Ncv1Bf$*vBu5g*ZB
          SPLUNK_HOST=localhost
          SPLUNK_PORT=8089
          SPLUNK_USERNAME=root
          SPLUNK_PASSWORD=KRAVIKIRAN
          EOF

      - name: Run Tests
        run: |
          pytest -v

      - name: Clean Up
        if: always()
        run: |
          rm -f config/.env

from config import (
    API_KEY,
    SPLUNK_HOST,
    SPLUNK_PORT,
    SPLUNK_USERNAME,
)

print("AbuseIPDB Key:", API_KEY[:8] + "...")
print("Splunk Host:", SPLUNK_HOST)
print("Splunk Port:", SPLUNK_PORT)
print("Splunk User:", SPLUNK_USERNAME)
